// Stub for raw ".data" asset requires under Jest. Returns a numeric asset id,
// mirroring how jest's React Native preset represents image assets, so
// `require('./codepush-update-meta.data')` resolves without bundling logic.
module.exports = 1;
